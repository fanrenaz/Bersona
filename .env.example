# Bersona 环境变量示例文件
# 将此文件复制为 .env 并按需修改 (例如: cp .env.example .env)
# 使用方法：在 shell 中 `export VAR=value` 或使用 python-dotenv 自动加载。

# === Skyfield 星历相关 ===
# 指定使用的星历文件；默认 de421.bsp (约20MB)。可选更高精度: de440s.bsp (~120MB)
BERSONA_EPHEMERIS=de421.bsp
# 自定义 Skyfield 缓存目录；为空则使用 ~/.skyfield
# SKYFIELD_CACHE_DIR=/path/to/.skyfield-cache

# === OpenAI / LLM 集成 ===
# OpenAI 兼容 API Key (必需才能启用 LLM 功能)
OPENAI_API_KEY=sk-your-api-key-here
# 或使用备用名称 (两者任选其一)
# OPENAI_KEY=sk-your-api-key-here
# 自定义 API 基地址 (兼容代理 / 第三方服务)，例如:
# OPENAI_BASE_URL=https://api.openai.com/v1
# 默认使用的模型名称；调用 astrology_describe/llm_chat 时可覆盖
OPENAI_MODEL=gpt-4o-mini

# === 地理编码 / 网络请求相关 ===
# 若需要 geopy 访问 OpenStreetMap，可配置自定义的 user agent 或代理
# GEOPY_USER_AGENT=bersona_geocoder
# HTTP(S) 代理示例 (若企业网络限制):
# HTTP_PROXY=http://127.0.0.1:7890
# HTTPS_PROXY=http://127.0.0.1:7890

# === 其他调试辅助 ===
# 控制是否打印星历首次下载提示 (默认打印)。设置为 0 关闭。
# BERSONA_QUIET=1
# 未来预留: 日志级别 (info/debug/warn)
# BERSONA_LOG_LEVEL=info

# === 示例使用说明 ===
# 1. 复制本文件为 .env
# 2. 修改 OPENAI_API_KEY 等实际值
# 3. 在 shell 中 `source .env` 或使用 python-dotenv:
#    from dotenv import load_dotenv; load_dotenv()
# 4. 运行: python -c "from bersona import Bersona; print(Bersona().llm_available)"

